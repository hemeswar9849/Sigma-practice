export const add = (a, b) => {
  return a + b;
};

export const sub = (a, b) => {
  return a - b;
};

export const mul = (a, b) => {
  return a * b;
};

// export default add;
//common js
// module.exports = {
//   add,
//   sub
// };

// ECMAScript Modules
