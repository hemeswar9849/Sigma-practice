var a = A => { }, b = _ => {
    console.log(_); const B = [..._.children].slice(1);
    for (const C of B) [...C.classList].includes('hide') ? C.classList.remove('hide') : C.classList.add('hide')
}, c = _a => { const _b = _a.ancestorTitles, _c = _a.status === 'passed', d = [0, 25, 50], g = document.createElement('div'), h = document.createElement('img'); let e = document.querySelector('#results'); _b.forEach((D, _B) => { let _C = document.querySelector(`#${e.id}-${D}`); if (!_C) { const _A = `${d[_B % _b.length]}px`; _C = document.createElement('div'); _C.id = `${e.id}-${D}`; const E = document.createElement('div'), _D = document.createElement('p'); E.addEventListener('click', () => b(_C)); E.classList.add(_B % 2 === 0 ? 'title-container-even' : 'title-container-odd'); const aA = document.createElement('img'); aA.style = `margin-left: ${_A};`; _D.innerText = ` ${D}`; _D.classList.add('title'); E.append(aA); E.append(_D); _C.append(E); e.append(_C) } const _d = _C.children[0].children[0]; _d.src = !_d.src.includes('failed') && _c ? `images/passed.png` : 'images/failed.png'; _B !== 0 && _C.classList.add('hide'); e = _C }); const f = document.createElement('div'); f.className = _c ? 'test-passed hide' : 'test-failed hide'; h.src = _c ? `images/passed.png` : 'images/failed.png'; const i = document.createElement('p'); i.className = 'test-description'; i.innerText = `${_a.title}`; if (!_c) { const { expected: aB, actual: aC } = _a.failureDetails[0].matcherResult; i.innerText += `\n\nExpected: ${JSON.stringify(aB)} \n Received: ${JSON.stringify(aC)}` } f.style = `padding-left: ${d[_b.length - 1] + 25}px; border-bottom: 1px solid #dad8da;`; g.className = 'test'; g.append(h, i); f.append(g); e.append(f) }; window.onload = async () => { var aD = await fetch('results.json').then(res => res.json()); for (const aE of aD.testResults[0].testResults) c(aE) }